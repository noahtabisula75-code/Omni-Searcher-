{
  "entities": {
    "ShareLink": {
      "title": "Share Link",
      "description": "A link that can be shared with a single device limit.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique link ID" },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the link was created" },
        "createdBy": { "type": "string", "description": "UID of the user who created the link" },
        "deviceId": { "type": "string", "description": "ID of the device that first used this link" },
        "isUsed": { "type": "boolean", "description": "Whether the link has been claimed by a device" },
        "expiresAt": { "type": "string", "format": "date-time", "description": "Optional expiration time" }
      },
      "required": ["id", "createdAt", "createdBy", "isUsed"]
    }
  },
  "firestore": {
    "/shareLinks/{linkId}": {
      "schema": "ShareLink",
      "description": "Collection of shareable links with device locking."
    }
  }
}
